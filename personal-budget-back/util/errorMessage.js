module.exports = {
  GENERAL: {
    requiredField: '{PATH} is required',
    minlength: '{PATH} shoud contains at least one {VALUE}',
    maxlength: "{PATH} can't contain more than {VALUE}",
  },
  USERSWEB: {
    userExist: '{VALUE} already exists',
  },
}
